# CLAUDE.md — МЕД ДПО / Алина (специфика)

> Общие правила — в корневом `CLAUDE.md`. Здесь специфика.

## Workflow
- **ID:** `jrnksVhHbVUMLql7`
- **Статус:** активен
- **Тип:** Telegram-бот (7 нод)
- **Назначение:** консультант компании ДПО для медработников. Квалификация → заявка → уведомление админу.

## Архитектура
```
Telegram Trigger → AI Agent (Алина) + OpenAI Chat Model + Simple Memory
                → Send to Client (HTTP, $env.TELEGRAM_MED_TOKEN)
                → If содержит "НОВАЯ ЗАЯВКА"?
                    ✅ Send to Admin (HTTP, 586613159)
                    ❌ конец
```

## Сценарий квалификации
1. Специальность клиента
2. Какая услуга интересует
3. Формат: очно / дистанционно
4. Срочность
5. Контакты (имя + телефон)

## Услуги и цены
| Услуга | Цена |
|---|---|
| Программы повышения квалификации | от 3 000 ₽ |
| Профессиональная переподготовка | от 15 000 ₽ |
| Первичная аккредитация | индивидуально |
| Периодическая аккредитация | от 15 000 ₽ |
| Отчёт и портфолио | от 7 000 ₽ |

## Credentials
- **OpenAI API** id `XYzdkSQVXxoUFMOK` — настроен на OpenRouter, модель `anthropic/claude-3-5-haiku`
- **Telegram API** — ручная привязка credential в ноде `Telegram Trigger` (нельзя через `$env`)

## Railway env vars
- `TELEGRAM_MED_TOKEN` — токен бота МЕД ДПО

## Грабли (исправленные)
- IF проверял `$json.text` (из Telegram-ответа). Правильно: `$('AI Agent').item.json.output`.
- OpenAI Chat Model без credentials — нужно явно назначить.
- Брендинг n8n в Telegram-ноде — заменено на HTTP Request с `$env`.

## Особенность ПДн (важно для продакшена!)
- Бот собирает имя + телефон → это **персональные данные** по 152-ФЗ.
- Для реального клиента нужна согласие на обработку ПДн + политика конфиденциальности.
- Supabase = US — для медицины это потенциальная проблема (трансграничная передача ПДн).

## Чек-лист после правок
1. `n8n_validate_workflow` обязательно.
2. Если меняли промпт Алины — прогнать 3–5 тестовых диалогов.
